This projects compilation uses gcc c++ package. <br>
<br>
Run the following command in terminal: <br>
```g++ -std=c++17 -Iinclude src/main.cpp -o build/run```<br>
<br>
After finishing you can execute it by:<br>
```./build/run```